<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Базовая тема для Compose приложения -->
    <!-- Не используется напрямую, так как Compose использует свою тему -->
    <style name="Theme.BishkekGuide" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>